%% parameter setting
timestep = 1;   % time step
mu = 0.2;       % coefficient of the distance regularization term R(phi)
iter_inner = 5;
lambda = 5;     % coefficient of the weighted length term L(phi)
alfa = 2.2;     % coefficient of the weighted area term A(phi)
epsilon = 1.5;  % paramater that specifies the width of the DiracDelta function
c0 = 2;
adaptive = 1;
sigma = 0.2;
border = 4;
